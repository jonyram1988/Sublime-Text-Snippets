<snippet>
	<content><![CDATA[
if(json.status){
    // mensaje de exito
    Apps.toastr_mensaje('success');
}else{
    // diferenciacion de mensajes errores
    if(json.userMessage || json.developerMessage){
        Apps.toastr_mensaje('error',json.userMessage);
    }else{
        Apps.toastr_mensaje('error');
    }
}
// actualizo datatable
wrapper.find('table').DataTable().draw();
]]></content>
	<tabTrigger>Mensajes</tabTrigger>
</snippet>
